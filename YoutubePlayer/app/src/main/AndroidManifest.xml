<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arun.youtubeplayer">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/youtube_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/youtube_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.YoutubePlayer">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".YoutubeActivity"
            android:exported="true"
            android:label="Youtube Player"/>
        <activity
            android:name=".StandaloneActivity"
            android:exported="true"
            android:label="Standalone Activity"/>
    </application>

</manifest>